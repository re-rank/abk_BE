# Cloudtype 배포 설정
# Dockerfile 사용 강제
app:
  name: abk-backend

build:
  type: dockerfile
  dockerfile: Dockerfile

env:
  - name: NODE_ENV
    value: production
  - name: PLAYWRIGHT_BROWSERS_PATH
    value: /app/.cache/ms-playwright
