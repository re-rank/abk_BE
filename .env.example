# ABK Backend 환경변수 설정
# 이 파일을 복사하여 .env로 이름 변경 후 실제 값을 입력하세요

# === Database - Supabase PostgreSQL ===
SUPABASE_DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT_ID].supabase.co:5432/postgres

# === JWT Authentication ===
JWT_SECRET=your-jwt-secret-key-here

# === Encryption Key (민감 정보 암호화용) ===
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=your-encryption-key-here

# === Upstash Redis (BullMQ) ===
REDIS_URL=rediss://default:[PASSWORD]@[HOST].upstash.io:6379

# === Upstash QStash (Scheduler) ===
QSTASH_TOKEN=your-qstash-token

# === Claude AI (Anthropic) ===
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# === Supabase ===
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# === Naver API ===
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# === Frontend URL (CORS) ===
FRONTEND_URL=https://your-frontend-domain.com

# === Backend URL (OAuth Callback) ===
BACKEND_URL=https://your-backend-domain.com

# === Browserless.io (원격 브라우저 서비스) ===
# https://www.browserless.io에서 가입 후 API Key 발급
BROWSERLESS_API_KEY=your-browserless-api-key

# === Environment ===
NODE_ENV=development
